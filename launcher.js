//==============================================================================
// 개발/라이브 서버 실행용 런처.
// src/main.js 를 동적 import 한다.
// main.js 는 자체적으로 엔진 기동 + engine.run 을 수행하므로 여기서는 import 만 하면 된다.
//
// 프로덕션 빌드 시에는 esbuild 가 src/main.js 를 직접 엔트리로 번들링하므로 이 파일은 사용되지 않는다.
//==============================================================================
import("./src/main.js").catch((error) => {
	console.error(`[launcher] 진입 파일 로드 실패: ./src/main.js`, error);
});
